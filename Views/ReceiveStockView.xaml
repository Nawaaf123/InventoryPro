<UserControl x:Class="InventoryPro.Views.ReceiveStockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Receive Stock"
                       FontSize="22"
                       FontWeight="Bold"/>
            <TextBlock Text="Add multiple products from a single BOL"
                       Foreground="Gray"/>
        </StackPanel>

        <!-- BOL + WAREHOUSE -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <StackPanel Margin="0,0,12,0">
                <TextBlock Text="BOL Number"/>
                <TextBox Width="200"
                         Text="{Binding BolNumber, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel>
                <TextBlock Text="Select Warehouse"/>
                <ComboBox Width="200"
                          ItemsSource="{Binding Warehouses}"
                          SelectedItem="{Binding SelectedWarehouse}"/>
            </StackPanel>
        </StackPanel>

        <!-- PRODUCTS TO RECEIVE -->
        <GroupBox Grid.Row="2" Header="Products to Receive">
            <DataGrid ItemsSource="{Binding ReceiveItems}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      Height="200">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="SKU" Binding="{Binding SKU}" Width="100"/>
                    <DataGridTextColumn Header="Product" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="80"/>
                </DataGrid.Columns>

            </DataGrid>
        </GroupBox>

        <!-- ACTION BUTTONS -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">

            <Button Content="Cancel"
                    Width="100"
                    Margin="0,0,8,0"
                    Command="{Binding CancelCommand}"/>

            <Button Content="Receive Stock"
                    Width="120"
                    Background="#10B981"
                    Foreground="White"
                    Command="{Binding ReceiveStockCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
