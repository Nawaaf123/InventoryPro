<UserControl x:Class="InventoryPro.Views.TransferStockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <StackPanel Margin="10" Width="420">

            <TextBlock Text="Transfer Stock Between Warehouses"
                       FontSize="18" FontWeight="Bold"
                       Margin="0,0,0,12"/>

            <TextBlock Text="Select Product"/>
            <ComboBox ItemsSource="{Binding Items}"
                      SelectedItem="{Binding SelectedItem}"
                      DisplayMemberPath="Name"
                      Margin="0,4,0,12"/>

            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="From Warehouse"/>
                    <ComboBox SelectedValue="{Binding FromWarehouse}" Margin="0,4,0,0">
                        <ComboBoxItem Content="A"/>
                        <ComboBoxItem Content="B"/>
                        <ComboBoxItem Content="C"/>
                        <ComboBoxItem Content="D"/>
                    </ComboBox>
                </StackPanel>

                <TextBlock Grid.Column="1" Text="→" VerticalAlignment="Bottom" HorizontalAlignment="Center" FontSize="18"/>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="To Warehouse"/>
                    <ComboBox SelectedValue="{Binding ToWarehouse}" Margin="0,4,0,0">
                        <ComboBoxItem Content="A"/>
                        <ComboBoxItem Content="B"/>
                        <ComboBoxItem Content="C"/>
                        <ComboBoxItem Content="D"/>
                    </ComboBox>
                </StackPanel>
            </Grid>

            <TextBlock Text="Quantity to Transfer"/>
            <TextBox Text="{Binding Quantity}" Margin="0,4,0,18"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Button Content="Cancel"
                        Command="{Binding CancelCommand}"
                        Margin="0,0,8,0"/>

                <Button Grid.Column="1"
                        Content="Transfer Stock"
                        Command="{Binding TransferCommand}"
                        Margin="8,0,0,0"/>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
