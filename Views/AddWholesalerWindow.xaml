<Window x:Class="InventoryPro.Views.AddWholesalerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Wholesaler"
        Height="420"
        Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>

            <TextBlock Text="Add Wholesaler"
                       FontSize="22"
                       FontWeight="Bold"
                       Margin="0,0,0,16"/>

            <TextBlock Text="Company Name"/>
            <TextBox Text="{Binding CompanyName}" Margin="0,4,0,12"/>

            <TextBlock Text="Contact Person"/>
            <TextBox Text="{Binding ContactPerson}" Margin="0,4,0,12"/>

            <TextBlock Text="Phone"/>
            <TextBox Text="{Binding Phone}" Margin="0,4,0,12"/>

            <TextBlock Text="Email"/>
            <TextBox Text="{Binding Email}" Margin="0,4,0,12"/>

            <TextBlock Text="Address"/>
            <TextBox Text="{Binding Address}" Margin="0,4,0,12"/>

        </StackPanel>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">

            <Button Content="Cancel"
                    Width="90"
                    Margin="0,0,8,0"
                    Command="{Binding CancelCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>

            <Button Content="Add"
                    Width="90"
                    Background="#10B981"
                    Foreground="White"
                    Command="{Binding SaveCommand}"/>
        </StackPanel>
    </Grid>
</Window>
