<UserControl x:Class="InventoryPro.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Title -->
            <StackPanel>
                <TextBlock Text="Inventory"
                           FontSize="24"
                           FontWeight="Bold"/>
                <TextBlock Text="Manage products across all warehouses"
                           Foreground="Gray"/>
            </StackPanel>

            <!-- ACTION BUTTONS (TOP RIGHT) -->
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="0,0,0,8">

                <Button Content="Receive Stock"
                        Margin="4"
                        Padding="10,4"/>

                <Button Content="Transfer Stock"
                        Margin="4"
                        Padding="10,4"/>

                <Button Content="+ Add Item"
                        Command="{Binding AddItemCommand}"
                        Background="#10B981"
                        Foreground="White"/>
                

            </StackPanel>
        </Grid>

        <!-- SEARCH / FILTER ROW -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,12,0,12">

            <TextBox Width="260"
                     Margin="0,0,8,0"
                     Text="Search by name or SKU..."/>

            <ComboBox Width="150" Margin="0,0,8,0"/>
            <ComboBox Width="150" Margin="0,0,8,0"/>
            <ComboBox Width="150"/>
        </StackPanel>

        <!-- DATA GRID -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Items}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="All">

            <DataGrid.Columns>
                <DataGridTextColumn Header="SKU" Binding="{Binding SKU}" Width="80"/>
                <DataGridTextColumn Header="Product Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="60"/>
                <DataGridTextColumn Header="A" Binding="{Binding WarehouseA}" Width="50"/>
                <DataGridTextColumn Header="B" Binding="{Binding WarehouseB}" Width="50"/>
                <DataGridTextColumn Header="C" Binding="{Binding WarehouseC}" Width="50"/>
                <DataGridTextColumn Header="D" Binding="{Binding WarehouseD}" Width="50"/>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>
