<Window x:Class="InventoryPro.Views.AddItemWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add New Item"
        Width="450"
        Height="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <ScrollViewer>
        <StackPanel Margin="20">

            <TextBlock Text="Add New Item"
                       FontSize="20"
                       FontWeight="Bold"
                       Margin="0,0,0,15"/>

            <TextBlock Text="Product Name"/>
            <TextBox Text="{Binding Name}" Margin="0,0,0,8"/>

            <TextBlock Text="SKU"/>
            <TextBox Text="{Binding SKU}" Margin="0,0,0,8"/>

            <TextBlock Text="Category"/>
            <TextBox Text="{Binding Category}" Margin="0,0,0,8"/>

            <TextBlock Text="Sub Category"/>
            <TextBox Text="{Binding SubCategory}" Margin="0,0,0,8"/>

            <TextBlock Text="Price"/>
            <TextBox Text="{Binding Price}" Margin="0,0,0,8"/>

            <TextBlock Text="Min Stock Level"/>
            <TextBox Text="{Binding MinStockLevel}" Margin="0,0,0,12"/>

            <TextBlock Text="Initial Stock" FontWeight="Bold"/>

            <UniformGrid Columns="2" Margin="0,6,0,12">
                <TextBox Text="{Binding WarehouseA}" />
                <TextBox Text="{Binding WarehouseB}" />
                <TextBox Text="{Binding WarehouseC}" />
                <TextBox Text="{Binding WarehouseD}" />
            </UniformGrid>

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">

                <Button Content="Cancel"
                        Width="80"
                        Margin="0,0,8,0"
                        Command="{Binding CancelCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>

                <Button Content="Add Item"
                        Command="{Binding AddItemCommand}"
                        Width="120"
                        Height="35"/>

            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</Window>
