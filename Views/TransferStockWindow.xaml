<Window x:Class="InventoryPro.Views.TransferStockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Transfer Stock"
        Height="350"
        Width="450"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ComboBox
             ItemsSource="{Binding Items}"
             DisplayMemberPath="Name"
             SelectedItem="{Binding SelectedItem}"
             Height="30"
             Margin="0,0,0,10"
             Foreground="Black"
             Background="White"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <ComboBox Width="150" ItemsSource="{Binding Warehouses}" SelectedItem="{Binding FromWarehouse}" Foreground="Black" Background="White"/>
            <TextBlock Text=" → " VerticalAlignment="Center" Margin="10,0"/>
            <ComboBox Width="150" ItemsSource="{Binding Warehouses}" SelectedItem="{Binding ToWarehouse}" Foreground="Black" Background="White"/>
        </StackPanel>

        <TextBox Grid.Row="2"
                 Text="{Binding Quantity}"
                 Margin="0,0,0,10"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Margin="0,0,10,0" Click="Close_Click"/>
            <Button Content="Transfer" Width="80" Command="{Binding TransferCommand}"/>
        </StackPanel>
    </Grid>
</Window>
